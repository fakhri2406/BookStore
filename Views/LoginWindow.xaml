<Window x:Class="FinalADO.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FinalADO"
        Title="Login" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Username:" Width="100"/>
            <TextBox Width="200" x:Name="UsernameTextBox"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Password:" Width="100"/>
            <PasswordBox Width="200" x:Name="PasswordBox"/>
        </StackPanel>
        <Button Grid.Row="2" Content="Login" Width="100" HorizontalAlignment="Right" Click="LoginButton_Click"/>
    </Grid>
</Window>
