<Window x:Class="FinalADO.Views.SetDiscountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinalADO.Views"
        mc:Ignorable="d"
        Title="Set Discount" Height="200" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,20">
            <Label Content="Discount (%) :" Width="150" VerticalAlignment="Center"/>
            <TextBox x:Name="DiscountTextBox" Width="200" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Save" Width="150" Height="50" Margin="10,0" Click="SaveButton_Click" Background="#4CAF50"/>
            <Button Content="Cancel" Width="150" Height="50" Margin="10,0" Click="CancelButton_Click" Background="#F44336"/>
        </StackPanel>
    </Grid>
</Window>
